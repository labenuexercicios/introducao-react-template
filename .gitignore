# As pastas e arquivos listados dentro do .gitignore são IGNORADAS pelo git. Ou seja, não irão para o repositório remoto (GitHub) na hora do push.

# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

node_modules
DS_Store
package-lock.json
build
.env